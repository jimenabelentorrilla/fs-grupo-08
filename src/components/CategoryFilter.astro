
<aside class="aside-filter">
    <ul class="menu-categories"> 
        <li>
            <button id="todos" class="button-menu active">Todos</button>
        </li>
        <li>
            <button id="clasicos" class="button-menu">Clásicos</button>
        </li>
        <li>
            <button id="pastas" class="button-menu">Pastas</button>
        </li>
        <li>
            <button id="minutas" class="button-menu">Minutas</button>
        </li>
        <li>
            <button id="postres" class="button-menu">Postres</button>
        </li>
    </ul>
</aside>


<style>
    /**Category Filter **/
    .aside-filter {
        display: flex;
        justify-content: center;
        padding-block: 2rem;
    }

    .menu-categories {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
        list-style: none;
        padding: 1rem 1.5rem;
        border-radius: 25px;
        background-color: oklch(0.95 0.02 110 / 0.9); /* suave, no tan gris cemento */
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.04);
        border: 1px solid oklch(0.9 0.01 230);
    }

    .button-menu {
        border: none;
        padding: 0.5rem 1.25rem;
        background-color: transparent;
        color: oklch(0 0 0);
        font-family: var(--font-family-text);
        font-size: 1rem;
        font-weight: 500;
        border-radius: 999px;
        transition: all 0.3s ease;
        cursor: pointer;
        position: relative;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .button-menu:hover {
        background-color: oklch(0.9 0.07 80 / 0.2);
        color: oklch(0.5 0.17 80);
    }

    .button-menu.active {
        background-color: oklch(0.85 0.14 80);
        color: #ffffff;
        font-weight: 600;
        box-shadow: 0 2px 8px oklch(0.8 0.15 80 / 0.4);
    }
    /* Tablets (≥768px) */
    @media screen and (min-width: 768px) {
        .aside-filter , .menu-categories{
            padding: 2rem;
        }
    }

    /* Desktops (≥992px) */
    @media screen and (min-width: 992px) {
        .aside-filter , .menu-categories{
            padding: 2rem;
        }
    }

    /* Large desktops (≥1200px) */
    @media screen and (min-width: 1200px) {
        .aside-filter , .menu-categories{
            padding: 2rem;
        }
    }
</style>